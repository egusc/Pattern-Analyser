A program that will take two files, a data file and a sequence file. The sequence file contains sequences that are
to be found in the data file. The program finds the number of patterns, and the indexes of where these patterns are.

To run in command line, Node.js must first be installed. To run the program, type:

node main.js

testlib.js provided as part of the project, main.js written by Ewan Guscott